# frozen_string_literal: true

# JSON representation of a Card object served by the API.
json.call(card, :name, :id, :card_type, :image_url, :multiverse_id, :artist, :cmc,
          :power, :subtype, :text, :flavor, :rarity)
