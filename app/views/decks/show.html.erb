<h1><%= @deck.name %></h1>

<ul>
  <% @cards.each do |card| %>
    <li>
      <%= card["name"] %>
    </li>
  <% end %>
</ul>

<h2>Add a Card:</h2>
  <div class="col-md-3">
    <%= render 'card_form' %>
  </div>
