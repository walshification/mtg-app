<% if @deck.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

    <ul>
      <% @deck.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= simple_form_for(@deck, html: {class: 'form-vertical' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :deck_type %>
  <%= f.input :color %>
  <%= f.input :legal_format %>
  <%= f.button :submit %>
<% end %>
