<h2>Add a card:</h2>

<% if @card.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

    <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= simple_form_for(@deck, html: {class: 'form-vertical' }) do |f| %>
  <%= f.association :cards, collection: Card.all %>
  <%= f.submit %>
<% end %>
