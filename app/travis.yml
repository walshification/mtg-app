language: ruby
sudo: false

addons:
  postgresql: "9.4"

before_install:
  - gem install bundler

env:
  - DB=postgresql

before_script:
  - bundle update
  - psql -c 'create database mtg-app_test' -U postgres

script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate test
